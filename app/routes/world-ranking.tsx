import { Outlet } from "@remix-run/react";

export default function WorkdRankingRoute() {
    return (
        <div>
          <main>
            <Outlet />
          </main>
        </div>
      );
}